import React from "react";
import { Button } from "react-bootstrap";
import "./MailOptions.css";

const Compose = () => {
  return <Button className="mailoptions"> Compose</Button>;
};

export default Compose;
